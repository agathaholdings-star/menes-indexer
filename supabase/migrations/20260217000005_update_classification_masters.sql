-- ============================================================
-- 分類マスタ更新: looks_type複数選択化 + body_types/cup_types 5段階化
-- ============================================================

BEGIN;

-- ============================================================
-- 1. looks_types: id=5 ラベル変更（妹系→ロリ系）
-- ============================================================
UPDATE looks_types SET label = 'ロリ系' WHERE id = 5;

-- ============================================================
-- 2. reviews: looks_type_id (int FK) → looks_type_ids (int[])
-- ============================================================
ALTER TABLE reviews DROP COLUMN looks_type_id;
ALTER TABLE reviews ADD COLUMN looks_type_ids int[];

-- ============================================================
-- 3. body_types: 4段階→5段階
--    旧: 1=スレンダー, 2=普通, 3=グラマー, 4=ぽっちゃり
--    新: 1=華奢, 2=スレンダー, 3=バランス, 4=グラマー, 5=ぽっちゃり
-- ============================================================
-- reviews 0件なのでFK違反なし
DELETE FROM body_types;
INSERT INTO body_types (id, label) VALUES
    (1, '華奢'),
    (2, 'スレンダー'),
    (3, 'バランス'),
    (4, 'グラマー'),
    (5, 'ぽっちゃり');

-- ============================================================
-- 4. cup_types: 4段階→5段階
--    旧: 1=ちょうどいい, 2=巨乳, 3=ちっぱい, 4=爆乳
--    新: 1=なし, 2=控えめ, 3=標準, 4=大きめ, 5=巨乳
-- ============================================================
DELETE FROM cup_types;
INSERT INTO cup_types (id, label) VALUES
    (1, 'なし'),
    (2, '控えめ'),
    (3, '標準'),
    (4, '大きめ'),
    (5, '巨乳');

COMMIT;
